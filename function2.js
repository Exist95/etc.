console.log(Math.round(1.6)); //2         라운드 = 반올림
console.log(Math.round(1.4)); //1

function sum(first, second){  // 매개변수 = parameter라고 부름
  console.log(first+second);
}

 sum(2,4); // 값 하나 하나를 arqument라고 함
